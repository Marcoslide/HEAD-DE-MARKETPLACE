/* Namespace compartilhado do Head Chat (Sprint 09.A) — padrão UMD do MIE:
   os MESMOS arquivos rodam em Node (require) e no navegador (<script>,
   globalThis.HEADCHAT). Uma implementação só — v3 e API consomem a mesma. */
'use strict';
module.exports = {};
